            <div class="row my-2">
                <div class="col-lg-12">
                    <a class="btn btn-success" href="{{ "{{ path('admin_" ~ route_name_prefix ~ "_new') }}" }}">
                        {{ "{{ 'create_new'|trans({'entity': '" ~ entity_singularized ~ "'}) }}" }}
                    </a>
                </div>
            </div>