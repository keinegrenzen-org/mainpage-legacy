{{ "{% trans_default_domain 'admin' %}" }}

{% block extends %}
    {{ "{% extends 'AdminBundle::base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
    {{ "{% block body %}" }}

    <div class="container">

        <div class="row">
            <div class="col-lg-12">
                <h1>{{ "{{ 'edit_or_new'|trans }}" }} {{ entity_singularized|capitalize }}</h1>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                {{ '{{ form_start(form) }}' }}
                {{ '{{ form_widget(form) }}' }}
                <div class="btn btn-success btn-form">
                    <input type="submit" value="Save"/>
                </div>
                {{ '{{ form_end(form) }}' }}
            </div>
        </div>
    </div>

    {% set hide_edit, hide_delete = true, false %}
    {{ include('crud/views/others/record_actions.html.twig.twig') -}}
    {{ "{% endblock %}" }}
{% endblock body %}
