{% trans_default_domain 'admin' %}

    {% extends 'AdminBundle::base.html.twig' %}

    {% block body %}

    <div class="container">

        <div class="row">
            <div class="col-lg-12">
                <h1>{{ 'edit_or_new'|trans }} Album</h1>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="btn btn-success btn-form">
                    <input type="submit" value="Save"/>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

            <div class="row">
        <div class="col-lg-12">
            <a class="btn btn-primary" href="{{ path('admin_album_index') }}">{{ 'back_to_list'|trans }}</a>            {{ form_start(delete_form, {'attr': {'class': 'btn btn-danger btn-form'}}) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}        </div>
    </div>{% endblock %}
