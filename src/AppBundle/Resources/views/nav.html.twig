<nav class="navbar">
    <div class="container-fluid nav-container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ (index)?'#':'/' }}"><img title="{{ 'mwb'|trans }}" src="{{ web_image(asset
                ('/bundles/app/images/logo_web.png')) }}"></a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ (index)?'':'/' }}{{ 'nav.projekt.href'|trans }}">{{ 'nav.projekt'|trans }}</a></li>
                <li><a href="{{ (index)?'':'/' }}{{ 'nav.donate.href'|trans }}">{{ 'nav.donate'|trans }}</a></li>
                <li><a href="{{ (index)?'':'/' }}{{ 'nav.artists.href'|trans }}">{{ 'nav.artists'|trans }}</a></li>
                <li><a class="cart-button" href="#"><span  class="fa fa-shopping-cart"><i class="sr-only">Cart</i></span></a></li>
            </ul>
            <div class="shopping-cart">
                <div class="cart-inner">
                    <h3>{{ 'cart.h3'|trans }}</h3>
                    <form action="{{ path('add_donation') }}" id="cart-form" method="post">
                    <ol class="cart-items">
                        <li class="empty-list">{{ 'noItems'|trans }}</li>
                    </ol>
                    <span class="cart-total">{{ 'totalMin'|trans }} <span>0,00</span>€</span>
                    <input type="submit" class="btn btn-primary btn-donate" value="{{ 'donate'|trans }}"/>
                    </form>
                </div>
            </div>
        </div><!-- /.navbar-collapse -->
    </div>
</nav>